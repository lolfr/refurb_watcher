requests==2.32.3
beautifulsoup4==4.12.3
lxml==5.3.0
Flask==3.0.3
Flask-Caching==2.3.0
tomli==2.0.2

# Optional — only for the MariaDB backend ([database] backend = "mariadb"):
#   pip install pymysql
